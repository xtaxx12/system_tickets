<!DOCTYPE html>
<html lang="es">
<head>
	<meta charset="UTF-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />
	<title><%= title %></title>
	<script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-50 text-slate-900">
	<header class="bg-slate-900 text-white">
		<div class="max-w-6xl mx-auto px-4 py-4 flex items-center justify-between">
			<h1 class="text-lg font-semibold">Crear Ticket de Soporte</h1>
			<nav class="space-x-4 text-sm">
				<a class="text-sky-200 hover:text-white" href="/tickets">Ver Tickets</a>
				<a class="text-sky-200 hover:text-white" href="/admin/login">Admin</a>
			</nav>
		</div>
	</header>
	<main class="max-w-3xl mx-auto px-4 py-8">
		<form action="/tickets" method="post" enctype="multipart/form-data" class="bg-white shadow rounded-lg p-6 space-y-5">
			<div>
				<label class="block text-sm font-medium mb-1">Nombre del solicitante *</label>
				<input class="w-full rounded-md border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-sky-500" type="text" name="requester_name" value="<%= data.requester_name || '' %>" required />
				<% if (errors.requester_name) { %><p class="text-sm text-red-600 mt-1"><%= errors.requester_name %></p><% } %>
			</div>
			<div>
				<label class="block text-sm font-medium mb-1">Área/Departamento *</label>
				<input class="w-full rounded-md border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-sky-500" type="text" name="department" value="<%= data.department || '' %>" required />
				<% if (errors.department) { %><p class="text-sm text-red-600 mt-1"><%= errors.department %></p><% } %>
			</div>
			<div>
				<label class="block text-sm font-medium mb-1">Tipo de Soporte *</label>
				<select class="w-full rounded-md border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-sky-500" name="support_type" required>
					<option value="">Seleccione...</option>
					<% SUPPORT_TYPES.forEach(t => { %>
						<option value="<%= t %>" <%= data.support_type === t ? 'selected' : '' %>><%= t %></option>
					<% }) %>
				</select>
				<% if (errors.support_type) { %><p class="text-sm text-red-600 mt-1"><%= errors.support_type %></p><% } %>
			</div>
			<div>
				<label class="block text-sm font-medium mb-1">Prioridad *</label>
				<select class="w-full rounded-md border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-sky-500" name="priority" required>
					<option value="">Seleccione...</option>
					<% PRIORITIES.forEach(p => { %>
						<option value="<%= p %>" <%= data.priority === p ? 'selected' : '' %>><%= p %></option>
					<% }) %>
				</select>
				<% if (errors.priority) { %><p class="text-sm text-red-600 mt-1"><%= errors.priority %></p><% } %>
			</div>
			<div>
				<label class="block text-sm font-medium mb-1">Asunto *</label>
				<input class="w-full rounded-md border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-sky-500" type="text" name="subject" value="<%= data.subject || '' %>" maxlength="120" required />
				<% if (errors.subject) { %><p class="text-sm text-red-600 mt-1"><%= errors.subject %></p><% } %>
			</div>
			<div>
				<label class="block text-sm font-medium mb-1">Descripción Detallada *</label>
				<textarea class="w-full rounded-md border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-sky-500" name="description" rows="6" required><%= data.description || '' %></textarea>
				<% if (errors.description) { %><p class="text-sm text-red-600 mt-1"><%= errors.description %></p><% } %>
			</div>
			<div>
				<label class="block text-sm font-medium mb-1">Subir Imagen (opcional)</label>
				<input class="block w-full text-sm text-slate-700 file:mr-4 file:py-2 file:px-3 file:rounded-md file:border-0 file:text-sm file:font-medium file:bg-slate-100 file:text-slate-700 hover:file:bg-slate-200" type="file" name="image" accept="image/*" />
			</div>
			<div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
				<div>
					<label class="block text-sm font-medium mb-1">¿Tiene AnyDesk instalado?</label>
					<select class="w-full rounded-md border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-sky-500" name="has_anydesk" id="has_anydesk">
						<option value="no" <%= (data.has_anydesk||'no')==='no' ? 'selected' : '' %>>No</option>
						<option value="yes" <%= data.has_anydesk==='yes' ? 'selected' : '' %>>Sí</option>
					</select>
				</div>
				<div id="anydesk_code_field" style="display:none;">
					<label class="block text-sm font-medium mb-1">Código AnyDesk</label>
					<input class="w-full rounded-md border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-sky-500" type="text" name="anydesk_code" value="<%= data.anydesk_code || '' %>" />
					<% if (errors.anydesk_code) { %><p class="text-sm text-red-600 mt-1"><%= errors.anydesk_code %></p><% } %>
				</div>
			</div>
			<div>
				<label class="block text-sm font-medium mb-1">Correo para confirmación (opcional)</label>
				<input class="w-full rounded-md border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-sky-500" type="email" name="email" placeholder="tu@correo.com" value="<%= data.email || '' %>" />
			</div>
			<div class="pt-2">
				<button class="inline-flex items-center gap-2 rounded-md bg-sky-600 px-4 py-2 text-white hover:bg-sky-700 focus:outline-none focus:ring-2 focus:ring-sky-500" type="submit">Crear Ticket</button>
			</div>
		</form>
	</main>
	<script>
		function toggleAnydesk(){
			const sel = document.getElementById('has_anydesk');
			document.getElementById('anydesk_code_field').style.display = sel.value === 'yes' ? 'block' : 'none';
		}
		document.getElementById('has_anydesk').addEventListener('change', toggleAnydesk);
		toggleAnydesk();
	</script>
</body>
</html>
