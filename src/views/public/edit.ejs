<!DOCTYPE html>
<html lang="es">
<head>
	<meta charset="UTF-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />
	<title><%= title %></title>
	<script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-50 text-slate-900">
	<header class="bg-slate-900 text-white">
		<div class="max-w-6xl mx-auto px-4 py-4 flex items-center justify-between">
			<h1 class="text-lg font-semibold">Editar Ticket <%= ticket.reference %></h1>
			<nav class="space-x-4 text-sm">
				<a class="text-sky-200 hover:text-white" href="/tickets/<%= ticket.reference %>">Volver al detalle</a>
			</nav>
		</div>
	</header>
	<main class="max-w-3xl mx-auto px-4 py-8">
		<form action="/tickets/<%= ticket.reference %>/editar?token=<%= ticket.edit_token %>" method="post" enctype="multipart/form-data" class="bg-white shadow rounded-lg p-6 space-y-5">
			<div>
				<label class="block text-sm font-medium mb-1">Nombre del solicitante</label>
				<input class="w-full rounded-md border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-sky-500" type="text" name="requester_name" value="<%= ticket.requester_name %>" />
			</div>
			<div>
				<label class="block text-sm font-medium mb-1">Área/Departamento</label>
				<input class="w-full rounded-md border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-sky-500" type="text" name="department" value="<%= ticket.department %>" />
			</div>
			<div>
				<label class="block text-sm font-medium mb-1">Tipo de Soporte</label>
				<select class="w-full rounded-md border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-sky-500" name="support_type">
					<% SUPPORT_TYPES.forEach(t => { %>
						<option value="<%= t %>" <%= ticket.support_type===t ? 'selected' : '' %>><%= t %></option>
					<% }) %>
				</select>
			</div>
			<div>
				<label class="block text-sm font-medium mb-1">Prioridad</label>
				<select class="w-full rounded-md border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-sky-500" name="priority">
					<% PRIORITIES.forEach(p => { %>
						<option value="<%= p %>" <%= ticket.priority===p ? 'selected' : '' %>><%= p %></option>
					<% }) %>
				</select>
			</div>
			<div>
				<label class="block text-sm font-medium mb-1">Asunto</label>
				<input class="w-full rounded-md border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-sky-500" type="text" name="subject" value="<%= ticket.subject %>" maxlength="120" />
			</div>
			<div>
				<label class="block text-sm font-medium mb-1">Descripción Detallada</label>
				<textarea class="w-full rounded-md border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-sky-500" name="description" rows="6"><%= ticket.description %></textarea>
			</div>
			<div>
				<label class="block text-sm font-medium mb-1">Actualizar Imagen (opcional)</label>
				<input class="block w-full text-sm text-slate-700 file:mr-4 file:py-2 file:px-3 file:rounded-md file:border-0 file:text-sm file:font-medium file:bg-slate-100 file:text-slate-700 hover:file:bg-slate-200" type="file" name="image" accept="image/*" />
				<% if (ticket.image_path) { %>
					<p class="mt-2 text-sm">Actual:</p>
					<img class="rounded-md max-w-xs" src="<%= ticket.image_path %>" />
				<% } %>
			</div>
			<div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
				<div>
					<label class="block text-sm font-medium mb-1">¿Tiene AnyDesk instalado?</label>
					<select class="w-full rounded-md border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-sky-500" name="has_anydesk" id="has_anydesk">
						<option value="no" <%= ticket.has_anydesk ? '' : 'selected' %>>No</option>
						<option value="yes" <%= ticket.has_anydesk ? 'selected' : '' %>>Sí</option>
					</select>
				</div>
				<div id="anydesk_code_field" style="display:none;">
					<label class="block text-sm font-medium mb-1">Código AnyDesk</label>
					<input class="w-full rounded-md border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-sky-500" type="text" name="anydesk_code" value="<%= ticket.anydesk_code || '' %>" />
				</div>
			</div>
			<div class="pt-2">
				<button class="inline-flex items-center gap-2 rounded-md bg-sky-600 px-4 py-2 text-white hover:bg-sky-700" type="submit">Guardar Cambios</button>
			</div>
		</form>
	</main>
	<script>
		function toggleAnydesk(){
			const sel = document.getElementById('has_anydesk');
			document.getElementById('anydesk_code_field').style.display = sel.value === 'yes' ? 'block' : 'none';
		}
		document.getElementById('has_anydesk').addEventListener('change', toggleAnydesk);
		toggleAnydesk();
	</script>
</body>
</html>
